---
name: my-workflow
status: approved
date: 2026-08-04
---

# my-workflow — 설계 스펙

## 목적

요구사항 문서화 → 설계/정책 반영 → 작업 분해 → 구현 → 회고로 이어지는 절차를, 검증된 폴더 구조와 문서 그대로 새 프로젝트에 이식하는 스캐폴딩 스킬. 매 프로젝트마다 이 절차를 새로 설계하지 않기 위한 것이며, 스킬 자체는 1회성 초기화(부트스트랩)에 가깝다 — 지속적으로 워크플로우를 "실행"하거나 상태를 관리하는 스킬이 아니다.

## 위치

`.claude/skills/my-workflow/` — 프로젝트 스코프 스킬. `~/.claude/skills/`에 두는 개인용 범용 스킬(예: `my-pm`)과 달리, 이식 대상 프로젝트마다 clone해서 `.git`을 제거하고 사용한다 (README "설치" 참고).

## 트리거

사용자가 직접 호출. "이 워크플로우 이식해줘", "요구사항 워크플로우 세팅해줘", "requirements/tasks 구조 만들어줘" 같은 자연어 트리거로 동작.

## 채택한 방법론 (명시적 스코프)

이 스킬은 범용 워크플로우 프레임워크가 아니라, **DDD(애그리거트/바운디드컨텍스트/유비쿼터스 언어) + ISO 주차(`YYYY-Www`) 단위 요구사항·작업 분리 관리**라는 특정 방법론을 전제로 한 스캐폴딩이다. 이름(`my-workflow`)만으로는 이 결합이 드러나지 않지만, `description`과 README에 DDD/ISO 주차가 이미 명시돼 있어 설치 전에 확인 가능하다고 보고, 이번 라운드에서는 방법론을 선택형으로 바꾸지 않고 그대로 유지하기로 결정했다.

## 단계별 동작

### 0. 대상 프로젝트 확인

- 현재 작업 디렉터리가 프로젝트 루트가 맞는지 확인(애매하면 사용자에게 확인).
- `requirements/`, `tasks/`, `docs/policies.md`, `docs/domain-model.md`, `CLAUDE.md` 5개 항목의 존재 여부를 이 단계에서 한 번에 확인한다. 하나라도 있으면 덮어쓰지 않고, 무엇이 있는지 보여주며 건너뛸지/병합할지 사용자에게 확인한다. **이 확인은 5개 항목 모두에 동일하게 적용된다** — 뒤 단계에서 항목별로 다른 조건을 추가로 판단하지 않는다.

### 1. requirements/, tasks/ 스캐폴드

- `requirements/README.md`, `tasks/README.md`를 `templates/`에서 그대로 복사.
- 실제 ISO 주차 폴더/파일은 이 시점에 만들지 않음 — 첫 요구사항이 들어올 때 생성.

### 2. docs/ 스텁

- `docs/policies.md`, `docs/domain-model.md`를 `templates/`에서 그대로 생성.
- 존재 여부는 0단계에서 이미 확인 완료 — 이 단계는 0단계의 결정을 그대로 따를 뿐, "다른 설계 문서가 있으면 스킵" 같은 별도 조건을 추가로 판단하지 않는다.

### 3. CLAUDE.md에 절차 섹션 추가

- 없으면 새로 생성, 있으면 기존 내용 끝에 `templates/CLAUDE-section.md` 추가(기존 내용은 건드리지 않음).
- 이미 유사한 "새 요구사항 처리 절차" 섹션이 있으면, 대체할지 나란히 둘지 사용자에게 확인 — 이는 0단계의 "파일 존재 확인"과는 별개로, "내용이 겹치는 섹션"을 다루는 것이라 별도 확인이 정당하다.

### 4. 마무리

- 무엇을 만들었고 무엇을 스킵했는지 요약.
- 커밋은 사용자가 명시적으로 요청할 때만 한다.

## 예외 처리

- 0단계에서 다섯 항목 중 하나라도 존재를 확인하면, 이후 단계는 그 자리에서 사용자가 정한 방침을 따른다 — 각 단계가 독자적으로 재확인하거나 다른 기준으로 스킵을 판단하지 않는다.
- CLAUDE.md에 이미 유사한 절차 섹션이 있는 경우만 예외적으로 3단계에서 별도 확인.
- README는 이 스킬이 직접 수정하지 않는다(별도 요청 시에만).
- 커밋은 사용자 명시적 요청 시에만.

## 검증 방법

이 스킬은 실행 코드가 아니라 프롬프트/지침 아티팩트이므로, 수동 드라이런으로 검증한다:
1. 아무것도 없는 빈 프로젝트에서 실행 — 5개 산출물(requirements/README.md, tasks/README.md, docs/policies.md, docs/domain-model.md, CLAUDE.md 섹션)이 모두 예상대로 생성되는지 확인.
2. `docs/domain-model.md`만 미리 있고 나머지는 없는 프로젝트에서 실행 — 0단계가 이 항목 하나만 콕 집어 확인하고, `docs/policies.md`를 포함한 나머지는 정상 생성하는지 확인 (항목별 독립적인 존재 확인이 실제로 되는지).
3. CLAUDE.md에 이미 "요구사항 처리 절차" 비슷한 섹션이 있는 프로젝트에서 실행 — 3단계의 대체/병행 확인이 실제로 뜨는지 확인.

## 명시적 비목표 (이번 라운드 범위 제외)

- 방법론 선택형 지원(DDD 스텁 생략 옵션 등) — 결합을 유지하기로 결정.
- PLAN.md(태스크별 구현 이력) — 가벼운 스캐폴딩 스킬 특성상 이번엔 SPEC.md만 유지하고 생략.
